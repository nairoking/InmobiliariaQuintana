@model IEnumerable<InmobiliariaQuintana.Models.Inmueble>

@{
    ViewData["Title"] = "Index";
}

<h1>Inmuebles</h1>

<p>
    <a asp-action="Create">Crear Inmueble</a>
</p>
<!--
    h3>Buscar Inmuebles disponibles</h3>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Index">
            <div class="form-group">
                <label class="control-label">Inicio de Contrato</label>
                <input type="date" name="FechaDesde" class="form-control" id="desde" />

                <label class="control-label">Fin De Contrato</label>
                <input type="date" name="FechaHasta" class="form-control" id="hasta" />
            </div>
            <div class="form-group">
                <input type="submit" value="Buscar" class="btn btn-primary" />
                <a class="btn btn-warning" asp-action="Buscar" asp-controller="Contrato" asp-route=""="Edit">Ver Solo Disponibles </a>
            </div>
        </form>
    </div>
</div>
-->

<p>
    <a class="btn btn-warning" asp-action="Disponibles" asp-controller="Inmuebles" title="Ver Disponibles">Ver Solo Disponibles </a>
</p>






<table class="table table-warning" id="Todos">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ambientes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Superficie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Latitud)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Longitud)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PropietarioId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Direccion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Ambientes)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Superficie)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Latitud)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Longitud)
                </td>
                <td>
                    @(item.Duenio.Nombre + " " + item.Duenio.Apellido)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>

                    <a class="btn btn-warning" asp-action="Edit" asp-controller="Inmuebles" asp-route-id="@item.IdInmueble" title="Editar"><i class="fas fa-pencil-alt"></i> </a>
                    <a class="btn btn-warning" asp-action="Details" asp-controller="Inmuebles" asp-route-id="@item.IdInmueble" title="Detalles"><i class="far fa-list-alt"></i> </a>
                    <a class="btn btn-warning" asp-action="Delete" asp-controller="Inmuebles" asp-route-id="@item.IdInmueble" title="Eliminar"> <i class="fas fa-trash-alt"></i></a>
                    <a class="btn btn-warning" asp-action="Contratos" asp-controller="Inmuebles" asp-route-id="@item.IdInmueble" title="Ver contratos"> <i class="fas fa-file-signature"></i></a>
                </td>
            </tr>



        }
    </tbody>
</table>
